import{strict as assert}from"node:assert";import fs from"node:fs";const root=new URL("../",import.meta.url),r=fs.readFileSync(new URL("app/api/payments/[id]/route.ts",root),"utf8");const checks=[["cross-authority",r.includes("readSettlementRefundAuthority(id)")],["settlement-read",r.includes("getSettlementCheckpointAuthoritative(id)")],["refund-read",r.includes("getRefundCheckpointsByPaymentIds([id])")],["fail-closed",r.includes('authority.outcome !== "CLEAR"')],["settlement-final",r.includes('durable.checkpoint.stage === "db_finalized" ? "settled_to_merchant" : "settlement_pending"')],["refund-final",r.includes('checkpoint.status === "completed" && checkpoint.stage === "audit_recorded" ? "refunded" : "refund_pending"')],["no-financial-writer",!r.includes("recordSettlement")&&!r.includes("advanceRefundCheckpoint")&&!r.includes("createRefund")],["no-horizon-submit",!r.includes("submitTransaction")&&!r.includes("Horizon.Server")],["no-direct-redis-repair",!r.includes("redis.set(`payment:${id}`")]];for(const[n,ok]of checks)assert.equal(ok,true,n);console.log(JSON.stringify({certification:"PASS",gate:"F3-3-DURABLE-PUBLIC-STATUS-AUTHORITY",mode:"CODE_READINESS",checks:checks.length,checksPassed:checks.length,financialMovementExecuted:false,redisProjectionRepairExecuted:false,failClosedOnDurableConflictOrUncertainty:true,nextGate:"F3-4-PROJECTION-SELF-HEALING"},null,2));