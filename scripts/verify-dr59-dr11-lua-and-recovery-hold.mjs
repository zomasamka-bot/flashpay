import fs from 'node:fs'
import { strict as assert } from 'node:assert'
const complete=fs.readFileSync('app/api/pi/complete/route.ts','utf8')
const recovery=fs.readFileSync('app/api/recovery/transient/route.ts','utf8')
const db=fs.readFileSync('lib/db.ts','utf8')
const luaStart=complete.indexOf('const atomicU2AResult = await redis.eval(`')
const luaEnd=complete.indexOf('`, [`payment:${flashPaymentId}`',luaStart)
assert.ok(luaStart>=0&&luaEnd>luaStart,'atomic U2A Lua not found')
const lua=complete.slice(luaStart,luaEnd)
for(const x of ["local dr11Candidate = ARGV[5] == '1'","local dr11Refund = dr11Candidate and (current.status == nil or current.status == 'pending')","local transitioningToPaidToApp = (current.status == nil or current.status == 'pending') and not dr11Refund","if dr11Refund then return 3 end"]) assert.ok(lua.includes(x),x)
assert.equal((lua.match(/local transitioningToPaidToApp/g)||[]).length,1)
assert.ok((lua.match(/transitioningToPaidToApp/g)||[]).length>=4)
assert.ok(recovery.includes('readDr11RefundCertificationHold(paymentId)'))
assert.ok(db.includes("certification_hold='dr11_refund'"))
assert.ok(!recovery.includes('const dr11ExactPaymentHold = await redis.get(`flashpay:certification:dr11:payment:${paymentId}`)'))
console.log(JSON.stringify({verdict:'PASS',gate:'DR59-DR11-LUA-CRASH-WINDOW-CLOSURE-SUPERSEDED-BY-DR60',luaFreeVariableDefectClosed:true,armedPaymentNeverTransitionsToPaidToApp:true,recoveryHonorsDurablePaymentHold:true,redisNoLongerAuthority:true,ordinaryPaymentsUnaffected:true,financialMovementAdded:false},null,2))
